#cloud-config
locale: en_US
timezone: "Europe/Budapest"
package_update: true
package_upgrade: true
packages:
  - ansible
runcmd:
  - [ wget, "https://raw.githubusercontent.com/zbalint/ansible-playbooks/master/alpine/alpine.ansible.yml", -O, "/run/alpine.ansible.yml" ]
  - [ ansible-playbook, /run/alpine.ansible.yml ]